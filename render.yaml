services:
  - type: web
    name: h1b-checker-api
    runtime: docker
    dockerfilePath: backend/Dockerfile
    region: ohio
    plan: free
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: CORS_ALLOWED_ORIGINS
        sync: false
      - key: PORT
        value: 8080
